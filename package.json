{
  "name": "purecore-semtempo-monorepo",
  "private": true,
  "description": "Monorepo for PureCore Temporal Graph engine and SemTempo GraphRAG semantic layer",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm --prefix packages/temporal-graph run build && npm --prefix packages/semtempo-graphrag run build",
    "build:bun": "bun run --filter '*' build",
    "test": "bun test",
    "test:node": "npm --prefix packages/temporal-graph run test:jest && npm --prefix packages/semtempo-graphrag run test:jest",
    "test:coverage": "bun test --coverage",
    "clean": "rimraf packages/*/dist packages/*/node_modules node_modules"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/suissa/purecore-semtempo-graphrag"
  },
  "author": "Suissa <suissa@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.0"
  }
}
